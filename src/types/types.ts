export interface TNFTs {
  name: string;
  imageUrl: string;
}

export interface TActiveChannels {
  name: string;
  url: string;
  imageUrl: string;
}

export interface TTopFollowers {
  name: string;
  pfp: string;
}
